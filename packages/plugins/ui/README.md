# UI Plugin (placeholder)

Empty shell for the UI testing plugin. Wired in Week 9 (dashboard + plugin orchestration).

Phase 1 discovery and test generation use `pluginId: "ui"` and `mode: "ui"` via the core agents.
